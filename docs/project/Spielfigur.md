# Spielfigur ([czg.objects.PlayerObject](../../src/main/java/czg/objects/Player.java)) **(noch nicht vorhanden)**

Die `Player`-Klasse erweitert die `BaseObject`-Klasse. Sie stellt **die einzige**
Instanz von sich über die statische Konstante `Player.INSTANCE` bereit.